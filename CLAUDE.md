# PNC Hand — Claude 项目协作指引

本文件记录用户对 Claude 的工作方式要求。以用户当前任务及已有授权为准；
只要求评审时先只读分析，已经要求修复/实现时由 Main Claude 执行关键修改，
不要把评审阶段的“先只读”当成永久禁止修改，也不要为已经授权的工作重复请求许可。

## 1. 先理解目标，再独立判断方案

最终需求：**RZ/V2H 直接通过共享 SPI 和 9 根独立 GPIO 片选采集 9 颗 RAA2S4704；
用户触摸左手机械手时，Foxglove 在三维手表面显示触摸位置与相对响应强弱，支持多个区域同时显示。**

- 保留原 Inspire RH56E2 串口控制、关节反馈、URDF/TF 和已有控制功能。
- 54 是电气通道槽（9 × 6），当前三维模板有 47 个物理区（掌部 22＋手指 25）。
- 轮询支持持续多点触摸，但不是所有通道同一时刻采样。
- 硬件未到位时用独立模拟推进软件联调；手势识别、触摸自动动作和相机叠加继续延期。

**保持独立，不受 Codex 的既有结论、修改风格或自我评价影响。**
Codex 写的代码、README、项目状态及测试记录都是评审材料，不是正确性的证明。
先理解项目背景、原有结构和数据流，再判断方案是否合理、是否足以实现最终需求。
不预设 Codex 正确，也不为反对 Codex 而推翻已有实现；以代码、实际差异、接口和可复现证据判断。
允许指出或纠正任何错误结论，并区分：原代码已有问题、本次引入问题、尚缺证据的假设。

## 2. 主对话与 subagent 分工

上下文有限，**可以自行创建或调用 subagent 执行不同的、有明确边界的任务**，
完成后向主对话汇报结果即可。无需为了常规任务拆分逐次询问用户。

```text
Main Claude
│ 只负责：
│ - 理解用户目标和范围
│ - 制定方案、做关键取舍
│ - 做关键修改并整合结果
│ - 维护当前项目状态
│
├── Explore / Research Agent
│   └── 搜代码、读大量文件、追踪调用链、查文档
│
├── Test / Debug Agent
│   └── 按明确问题编译、跑必要测试、分析大量日志
│
└── Review Agent
    └── 最后检查 diff、bug、接口破坏和 regression
```

这些是职责划分，可使用当前环境支持的内置或临时 subagent，不强制特定模型或插件。

- Main Claude 保留全局判断，阅读关键证据；大量文件扫描和日志消化交给对应 agent。
- 每次委派写清：目标、文件/模块范围、必要背景、只读或可写边界、停止条件、汇报格式。
  不假设子代理已经继承主对话或读过本文件，必须传递它完成任务所需的约束。
- 探索/评审默认只读；测试代理负责验证与定位，业务代码的关键修改由 Main Claude 掌握。
  必须委派辅助文件修改时，明确独占范围，避免多个 agent 同时改同一文件。
- 子代理仅返回：结论、文件与行号、实际做过的检查及结果、风险/不确定性、必要下一步。
  不向主对话粘贴整份源码、完整文档或巨量日志；大输出保留在文件中并给路径。
- 相互独立的工作可并行，依赖项顺序执行。简单问题直接处理，不为分工而启动所有角色。
- Review Agent 在初次评估时检查现有实现 diff；发生修改后再检查最终 diff 和回归风险。

## 3. 必须先读原有包，再看 Codex 的接入是否正确

先读 [项目背景](README.md) 和 [当前状态](PROJECT_STATUS.md)，
评审时按需读 [独立评审指南](docs/REVIEW_GUIDE.zh-CN.md)，详细启动方式见 [Foxglove 说明](foxglove/README.md)。
按需打开相关章节，避免为节省人工阅读而把全部文档导入主对话上下文。

### 原机械手基础

| 包 | 必须理解的职责 |
| --- | --- |
| `src/robots/inspire_rh56e2_hand_ros2_control` | 原串口协议、硬件 `read/write`、状态/命令接口 |
| `src/robots/inspire_rh56e2_hand_bringup` | 原 launch、controller manager、控制器参数与真实/模拟选择 |
| `src/robots/inspire_rh56e2_hand_description` | 关联的 URDF/Xacro、关节、坐标系与模型 |
| `src/utils/dexhand_utils` | 关联的 gripper adapter 与关节命令转换 |

### 原触觉链路三个包

| 包 | 必须理解的职责 |
| --- | --- |
| `src/robots/ssc_tactile_hand_ros2_control` | SPI/GPIO、9 颗芯片扫描、通道对应、EMA/tare、失败与恢复 |
| `src/robots/ssc_tactile_hand_bringup` | SPI/CS 配置、Xacro 状态接口、触觉 controller manager 和启动 |
| `src/utils/state_interfaces_broadcaster` | 将原状态接口发布为 `names/values`，含顺序、QoS 与时间戳 |

之后再检查新增 `src/utils/pnc_tactile_visualizer`、`src/utils/pnc_hand_demo` 和
`foxglove/foxglove_inspire_hand_panels` 如何接入上述基础；不能只看新 UI 或模拟效果。

核对实际分支、HEAD、工作区差异和 PR 状态。实现基线为
`3ecf1d6891a4dd8057e88c578c4c514a67e420c0`，最初 Foxglove 实现提交为
`e5c43263a4517076c60ad469f3b99fc8256eb4e2`，后续改动按实际 diff 重新判断。
[PR #1](https://github.com/Jrin111/PNC_hand/pull/1) 的工作分支是 `codex/pnc-foxglove-offline`。
PR 分支是完整仓库快照，包含继承的基础代码；“Files changed”只显示差异。
不要只读 PR 差异或假设 `main` 已含全部新功能，必须打开原文件理解上下文。

起始实现的核对线索：原驱动/broadcaster 的 C++ 未修改；运动 bringup 改了
`launch_foxglove` 开关及 mock 仅发布 position。这些仍须用提交差异核实，不能直接当作结论。
重点回答：新增层是否正确复用原 controller、接口、namespace、QoS、TF 和启动关系？
是否影响原机械手真实控制？真实数据能否不依赖模拟源到达 Foxglove？
若发现基础结构问题，说明触发条件与最小修复，不为统一风格大幅重构或重命名原包。

## 4. 适量验证，不做无目的的大量测试

- 优先静态阅读、调用链和 diff 检查。每次编译/测试先明确要消除哪个具体疑问。
- 根据风险选择最小验证范围；不默认全仓重编译、反复完整模拟、长时间压测或新增重复测试。
- 文档或无关资源修改不触发全量 C++ 构建；接口/运行逻辑有变化时做对应的必要验证。
- 只有确认存在剩余风险才扩大测试；得到足够证据后停止。不能用“减少测试”掩盖未验证风险。
- Test / Debug Agent 压缩输出，保留命令、环境、关键错误、日志路径及结果；
  说明是历史报告、当前复跑还是尚未执行，不能把计划写成已通过。
- 模拟验证只能支持消息/显示/mock 逻辑，不能证明真实 SPI、ADC、40 Hz 性能或机械手反馈。
- 真实 target 是 RZ/V2H AArch64 + ROS 2 Jazzy。原 `install/` 保留旧目标产物，
  不含新增 Python 显示包和本次 launch；开发安装使用单独目录，不覆盖旧产物。
  未变更的兼容 C++ 产物可复用；若改原生接口/ABI，再用匹配 xbuild 构建相关依赖集合。

## 5. 主对话汇总与维护状态

Main Claude 汇总子代理证据，用中文直接回答：方案是否合理、在什么条件下能实现目标、
哪里修改正确或错误、还缺哪些关键工作。问题提供文件/行号、影响及最小建议；不堆日志或泛化长报告。

完成关键判断或修改后更新 `PROJECT_STATUS.md`：当前目标、实际代码变更、验证证据、
未解决问题、所需资料、下一步及提交/构建对应关系。改变用法时同步 README。
明确区分“源码已实现”“模拟已验证”“目标板待部署”“硬件待验收”；
没有实物依据的映射、量程和时序不标记为完成。

在上下文切换或交接前留下简短状态：目标、当前分支/提交、已完成项、关键决策、
已发现风险、未解决项、下一步及必要文件/日志路径，避免下一轮从头阅读。
提交、推送、合并、目标部署和真实机械手动作按用户已有授权与任务范围执行；
本文件描述协作方式，不自行扩大用户要求。
